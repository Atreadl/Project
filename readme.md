Сначала запустить script.m, затем модель harvester.slx.

Для генерации в c++ использовать Embedded coder, должна получиться папка harvester_ert_rtw с main.cpp файлом.
main.cpp в корне лежит в качестве примера.